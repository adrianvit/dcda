function Product(product_id, description, price, quantity, host) {
				var self = this;
				self.product_id = product_id;
				self.description = description;
				self.price = price;
				self.quantity = quantity;
				self.host = host;
			};